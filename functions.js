// functions -- controla do fluxo de dados entrada saida de dados
//verbo + substantivo

/* let corSite = "azul";
function resetaCor(){
    corSite = " ";
};
console.log(corSite);
resetaCor();
console.log(corSite); */

/* let corSite = "azul";
function resetaCor(cor, tonalidade){
    corSite = cor + ' ' + tonalidade; // + junta o texto concatena
};

console.log(corSite);
resetaCor("vermelho", "claro" );
console.log(corSite) */

// funcao pode ser de 2 tipos 

//realiza uma tarefa e nao devolve nada

/* function dizeNome() {
    console.log('Polly');
}
dizeNome();

// realiza uma tarefa e devolve 

function MultiplicarPorDois(valor) {
    return valor * 2;
}
//console.log(MultiplicarPorDois(5)); 

let resultado = MultiplicarPorDois(5)
console.log(resulet numero*/

let numero = 50;

    if (numero > 0){
       console.log('Numero positivo'); 
    } else if (numero < 0){
       console.log('Numero negativo');
    }else (numero = 0);
       console.log('Numero nulo');

